rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The browser never accesses mission memory directly. Cloud Run uses IAM.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
